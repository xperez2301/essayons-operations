Flask==3.0.3
Werkzeug==3.0.3
gunicorn==23.0.0
python-dotenv==1.0.1
openpyxl==3.1.5
pypdf==4.3.1
requests==2.32.3

# Optional: only needed for the RMS scraping/automation features.
# On Azure App Service this also requires `playwright install chromium`
# (see README). The app runs fine without it; RMS automation is just disabled.
playwright==1.48.0
